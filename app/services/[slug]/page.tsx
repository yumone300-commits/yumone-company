import {notFound} from 'next/navigation';
import {services} from '@/data/services';
import {site} from '@/data/site';
import {Container,PageHero,SectionTitle,FinalCTA,Breadcrumb,Button} from '@/components/ui';
import {metadata,JsonLd,BreadSchema} from '@/lib/seo';
export function generateStaticParams(){return services.filter(s=>s.slug!=='franchise').map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===slug);return s?metadata(s.title,s.summary,`/services/${slug}`):{}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(s=>s.slug===slug);if(!s)notFound();const crumbs=[{label:'사업분야',href:'/services'},{label:s.short,href:`/services/${slug}`}];return <><PageHero eyebrow={s.eyebrow} title={s.title} description={s.summary} image={s.image}/><Breadcrumb items={crumbs}/><section className="section"><Container className="split"><div><p className="eyebrow">IS THIS YOUR CHALLENGE?</p><h2>이런 고민이 있다면<br/>함께 점검해 보세요.</h2><Button href={`/contact?service=${slug==='ai-search'?'ai':slug}`}>이 서비스 상담하기</Button></div><ul className="check-list">{s.problems.map(p=><li key={p}>{p}</li>)}</ul></Container></section><section className="section soft"><Container><SectionTitle eyebrow="WHAT WE DO" title="브랜드에 필요한 실행을 설계합니다."/><div className="detail-grid">{s.deliverables.map((d,i)=><div className="detail-card" key={d}><span>0{i+1}</span><h3>{d}</h3></div>)}</div><p className="content-note">세부 범위와 산출물은 브랜드 현황 및 상담 결과에 따라 조정합니다.</p></Container></section><section className="section"><Container><SectionTitle eyebrow="OUR PROCESS" title="진단부터 실행까지, 함께합니다."/><div className="steps">{s.steps.map((d,i)=><div className="step" key={d}><span>STEP 0{i+1}</span><h3>{d}</h3></div>)}</div></Container></section><section className="section soft"><Container><SectionTitle eyebrow="FAQ" title="자주 묻는 질문"/><div className="faq">{s.faq.map(f=><details key={f.q}><summary>{f.q}</summary><p>{f.a}</p></details>)}</div></Container></section><FinalCTA/><BreadSchema items={crumbs}/><JsonLd data={{'@type':'Service',name:s.title,description:s.summary,url:site.url+`/services/${slug}`,provider:{'@id':site.url+'/#organization'}}}/><JsonLd data={{'@type':'FAQPage',mainEntity:s.faq.map(f=>({'@type':'Question',name:f.q,acceptedAnswer:{'@type':'Answer',text:f.a}}))}}/></>}
